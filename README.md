# LFX++
GIT repository for the Linear Function X++ language, created from the X++ language, developed in the LProg 24/25 course.

To build the executable, run `makeit.sh`. To run all tests, run `run_tests.sh`.
